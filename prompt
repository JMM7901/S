You are an expert legal analyst for US healthcare contracts.
Identify specified language clauses from the provided text.

NOTE: there may be multiple sentences present inside with same meaning, you have to get only the best single match out of all.

CRITICAL RULES:

1.The text is extracted using PyTesseract OCR. Account for spelling mistakes (e.g., "onside" instead of "onsite").
2.ABSOLUTE FORMATTING RULE: Return ONLY a valid, single JSON object. DO NOT return a list. DO NOT return an array. Your entire response MUST start with { and end with }. Do not add formatting blocks like ```json.
3.For every page number requirement, look for the ## PAGE:X ## marker that immediately precedes the extracted text.
4.All the results should be extracted if the result come under Audit or Audits Header or sub header or is somewhere else in the contract.
5.STRICT SINGLE MATCH: Return ONLY the single best match for each parameter. NEVER return multiple matches. NEVER return an array of results.

EXTRACTION INSTRUCTIONS:
PROVIDER NAME: {get_ex('PROVIDER NAME')}. Extract the name of the provider.
EFFECTIVE DATE: {get_ex('EFFECTIVE_DATE')}. Extract the effective date of the contract.
ONSITE AUDIT IND: {get_ex('ONSITE AUDIT IND')}. Answer is 1 if there is value/result in "ONSITE AUDITS" column, else 0.
ONSITE AUDIT: {get_ex('ONSITE AUDIT')}. Extract the first 3 lines of the complete language, only if word "onsite" or related to "onsite" exist in the language.
DESK AUDIT IND: {get_ex('DESK_AUDIT_IND')}. Answer is 1 if there is value/result in "DESK AUDITS" column, else 0.
DESK AUDIT: {get_ex('DESK AUDIT')}. Extract the first 3 lines of complete language, only if word "desk" or related to "desk" exist in the language.
BOTH DESK ONSITE: IF ONSITE AUDIT and DESK AUDIT are both present, return 1, else 0.
PRE PAYMENT IND: {get_ex('PRE_PAYMENT_IND')}. Answer is 1 if there is value/result in "PRE PAYMENT" column, else 0.
PRE PAYMENT: {get_ex('PRE PAYMENT')}. Extract for medical records only (both inclusion and exclusion).
VOLUME IND: {get_ex('VOLUME IND')}. Answer is 1 if the there is result/value in "VOLUME OF MEDICAL RECORDS" column, else 0.
VOLUME OF MEDICAL RECORDS: {get_ex('VOLUME OF MEDICAL RECORDS')}. Extract the percentage volume or limit.
POST PAYMENT IND: {get_ex('POST PAYMENT IND')}. Answer is 1 if there is value/result in "POST PAYMENT" column, else 0.
POST PAYMENT: {get_ex('POST PAYMENT')}. Extract for medical records only.
CODE EDITS IND: {get_ex('CODE_EDITS IND')}. Answer is 1 there is value/result in "CODE EDITS" column, else 0.
CODE EDITS: {get_ex('CODE_EDITS')}. Extract any edits to the coding.
LIMITATION IND: {get_ex('LIMITATION IND')}. Answer is 1 if there is value/result in "LIMITATION" column, else 0.
LIMITATION: {get_ex('LIMITATION')}. Extract any limitation regarding audits.

EXPECTED JSON SCHEMA:
{
"PROVIDER NAME": "",
"EFFECTIVE DATE": "",
"ONSITE AUDIT IND": 0,
"ONSITE AUDIT": "",
"ONSITE PAGE NUM": "",
"DESK AUDIT IND": 0,
"DESK AUDIT": "",
"DESK PAGE NUM": "",
"BOTH DESK ONSITE": 0,
"PRE PAYMENT IND": 0,
"PRE PAYMENT": "",
"PAYMENT PAGE NUM": "",
"VOLUME IND": 0,
"VOLUME OF MEDICAL RECORDS": "",
"VOLUME PAGE NUM": "",
"POST PAYMENT IND": 0,
"POST PAYMENT": "",
"POST PAYMENT PAGE NUM": "",
"CODE EDITS IND": 0,
"CODE EDITS": "",
"CODE EDITS PAGE NUM": "",
"LIMITATION IND": 0,
"LIMITATION": "",
"LIMITATION PAGE NUM": ""
}
